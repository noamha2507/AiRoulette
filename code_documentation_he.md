# תיעוד קוד - קזינו רולטה AI

מסמך זה מרכז את מבנה הפרויקט, הסבר על התיקיות, פירוט המחלקות ולוגיקת המשחק (פזאדו-קוד) בשפה העברית.

---

## 📂 מבנה הפרויקט והתיקיות

### מבנה הקבצים:
```text
lastproject/
├── main.py              # קובץ הכניסה הראשי (Entry Point) - מנהל את ה-UI והלולאה המרכזית.
├── MainController.py    # בקר המשחק - מקשר בין המודלים, מסד הנתונים וה-AI.
├── models.py            # הגדרת אובייקטי המשחק (שחקן וגלגל).
├── database.py          # ניהול שמירת נתונים (SQLite) והיסטוריית משחקים.
├── casino.db            # קובץ מסד הנתונים (נוצר אוטומטית).
├── __pycache__/         # תיקייה פנימית של פייתון המכילה קבצים מקומפלים (לשיפור ביצועים).
└── temp_venv/           # סביבה וירטואלית זמנית להרצת הפרויקט ומניעת התנגשויות בחבילות.
```

---

## 🏗️ פירוט המחלקות (Classes)

### 1. `Player` (מתוך `models.py`)
**תפקיד:** מייצגת את המשתמש במשחק ומנהלת את המאזן הכספי שלו.
- **תכונות:**
    - `name`: שם השחקן.
    - `balance`: כמות הכסף הנוכחית של השחקן.
- **מתודות עיקריות:**
    - `get_balance()`: מחזירה את המאזן הנוכחי.
    - `deposit(amount)`: הוספת כסף לחשבון.
    - `withdraw(amount)`: משיכת כסף (בודקת אם יש מספיק יתרה).

### 2. `Wheel` (מתוך `models.py`)
**תפקיד:** מדמה גלגל רולטה אירופאי עם 37 מספרים (0-36).
- **תכונות:**
    - `numbers`: מילון הממפה כל מספר לצבע שלו (ירוק ל-0, אדום/שחור לאחרים).
- **מתודות עיקריות:**
    - `spin()`: מגרילה מספר אקראי ומחזירה אותו יחד עם הצבע שלו.

### 3. `DatabaseManager` (מתוך `database.py`)
**תפקיד:** אחראית על שמירת מצב השחקן והיסטוריית ההימורים.
- **מתודות עיקריות:**
    - `load_player_data(username)`: טעינת יתרה קיימת או יצירת שחקן חדש עם $1000.
    - `save_game(...)`: רישום כל הימור, התוצאה שלו וזמן ביצועו.
    - `get_player_history(username)`: שליפת ההימורים האחרונים לצורך תצוגה.

### 4. `MainController` (מתוך `MainController.py`)
**תפקיד:** ה"מוח" של המערכת. הוא מקבל פקודות מה-UI ומפעיל את הלוגיקה המתאימה.
- **מתודות עיקריות:**
    - `get_ai_insight()`: פונה למודל AI (Ollama) לקבלת הערה משעשעת או תובנה מהדילר.
    - `process_bet(type, selection, amount)`: מחשבת אם השחקן ניצח, מעדכנת את הכסף בבסיס הנתונים ומחזירה הודעת תוצאה.

### 5. `SessionStats` (מתוך `main.py`)
**תפקיד:** ניהול סטטיסטיקה בזמן אמת עבור הדיספליי (דשבורד).
- **תכונות:** סופרת זכיות, הפסדים, ומזהה "מספרים חמים".

---

## 💡 לוגיקת המשחק (פזאדו-קוד)

להלן תיאור התהליך המרכזי שקורה בתוך `main.py`:

```text
1. התחלה:
   - טען את מסד הנתונים.
   - בקש מהמשתמש שם.
   - משוך את יתרת השחקן (או צור שחקן חדש).

2. לולאת המשחק המרכזית (כל עוד השחקן לא יצא):
   א. נקה מסך והצג לוגו ודשבורד סטטיסטי.
   ב. הדילר (AI) אומר משפט פתיחה מעודד או עוקצני.
   ג. בקש מהמשתמש סוג הימור:
      - מספר בודד (0-36).
      - צבע (אדום/שחור).
      - זוגי/אי זוגי.
   ד. בקש סכום הימור (ודא שיש מספיק כסף).
   ה. הפעל אנימציית סיבוב של הגלגל.
   ו. ה-Controller מחשב תוצאה:
      - אם (סוג ההימור == תוצאת הגלגל):
          הוסף כסף למאזן (פי 35 למספר, פי 2 לצבע/זוגיות).
          סמן כניצחון.
      - אחרת:
          הורד את סכום ההימור מהמאזן.
          סמן כהפסד.
   ז. עדכן את מסד הנתונים בהיסטוריה ובמאזן החדש.
   ח. שאל את המשתמש אם ברצונו להמשיך או לצאת.

3. סיום:
   - הצג סיכום הישגים.
   - סגור חיבור למסד הנתונים וצא.
```
